/** Host loader entry for the header environment selector browser implementation exported from `./client`. */

/** Host plugin body — no host-side behavior for the header dropdown. */
export function apply(): void {}
